# Автоматическая регистрация пользователей

## Обзор

Реализована система автоматической регистрации пользователей при первом входе в систему. Пользователи автоматически регистрируются в базе данных при аутентификации.

## Функциональность

### ✅ Автоматическая регистрация

1. **При первом входе** - пользователь автоматически регистрируется в БД
2. **Проверка существования** - система проверяет, существует ли пользователь
3. **Двойная регистрация** - пользователь регистрируется в двух таблицах:
   - `User` - основная таблица пользователей
   - `KerberosUser` - таблица Kerberos пользователей

### 🔧 Логика работы

1. **Аутентификация** - пользователь входит в систему
2. **Проверка БД** - система проверяет существование пользователя
3. **Автоматическая регистрация** - если пользователь не найден, создается запись
4. **Определение роли** - роль определяется на основе имени пользователя
5. **Определение отдела** - отдел определяется автоматически

## Реализация

### Модуль аутентификации

**Файл**: `backend/simplified_real_kerberos_auth.py`

#### Новые методы:

```python
def _auto_register_user(self, username: str):
    """Автоматическая регистрация пользователя в БД"""
    # Проверка существования в User
    # Создание записи в User если не существует
    # Проверка существования в KerberosUser
    # Создание записи в KerberosUser если не существует

def _get_user_department(self, username: str) -> str:
    """Определение отдела пользователя на основе имени"""
    # Логика определения отдела по имени пользователя
```

#### Интеграция:

- **Windows Auth fallback** - автоматическая регистрация при Windows Auth
- **Kerberos Auth** - автоматическая регистрация при Kerberos Auth

### API Endpoints

**Файл**: `backend/api.py`

#### Новые endpoints:

```python
@api_bp.route('/users/register', methods=['POST'])
def register_user():
    """Ручная регистрация пользователя (для тестирования)"""

@api_bp.route('/users/check-registration', methods=['GET'])
def check_user_registration():
    """Проверить, зарегистрирован ли текущий пользователь"""
```

## Логика определения ролей и отделов

### Роли пользователей

```python
admin_users = [
    'admin', 'administrator', 'root', 'manager',
    'админ', 'администратор', 'руководитель',
    'system', 'service'  # Системные пользователи
]

# Все остальные пользователи получают роль 'user' по умолчанию
```

### Отделы пользователей

```python
def _get_user_department(self, username: str) -> str:
    if username.lower() in ['admin', 'administrator', 'root']:
        return 'IT отдел'
    elif username.lower() in ['manager', 'руководитель']:
        return 'Управление'
    elif username.lower() in ['пользователь', 'user']:
        return 'IT отдел'
    else:
        return 'Общий отдел'
```

## Структура данных

### Таблица User

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(100) UNIQUE NOT NULL,
    full_name VARCHAR(200) NOT NULL,
    department VARCHAR(100) NOT NULL,
    email VARCHAR(200) UNIQUE,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица KerberosUser

```sql
CREATE TABLE kerberos_users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(100) UNIQUE NOT NULL,
    principal VARCHAR(200) UNIQUE NOT NULL,  -- username@REALM
    realm VARCHAR(100) NOT NULL,
    full_name VARCHAR(200),
    department VARCHAR(100),
    email VARCHAR(200),
    role VARCHAR(20) NOT NULL DEFAULT 'user',
    is_active BOOLEAN DEFAULT TRUE,
    last_login DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## Результаты тестирования

### ✅ Успешные тесты

1. **Автоматическая регистрация** - новые пользователи регистрируются автоматически
2. **Проверка существования** - система корректно определяет существующих пользователей
3. **Двойная регистрация** - пользователи регистрируются в обеих таблицах
4. **Определение ролей** - роли определяются корректно
5. **Определение отделов** - отделы назначаются автоматически

### 📊 Статистика

- **Всего пользователей**: 6 (включая автоматически зарегистрированных)
- **Активных пользователей**: 6
- **Пользователей в KerberosUser**: 5

### 🎯 Примеры регистрации

#### Новый пользователь `test_simple_user` (роль: user):

**User таблица:**
- ID: 6
- Username: test_simple_user
- Full name: test_simple_user
- Department: Общий отдел
- Email: test_simple_user@company.com
- Active: True

**KerberosUser таблица:**
- ID: 5
- Username: test_simple_user
- Principal: test_simple_user@EXAMPLE.COM
- Realm: EXAMPLE.COM
- Role: user
- Department: Общий отдел
- Active: True

#### Пользователь `admin` (роль: admin):

**User таблица:**
- Username: admin
- Department: IT отдел
- Role: admin

**KerberosUser таблица:**
- Username: admin
- Principal: admin@EXAMPLE.COM
- Role: admin
- Department: IT отдел

## Логирование

Все события регистрации записываются в лог:

```
✅ Новый пользователь зарегистрирован: test_simple_user
✅ Kerberos пользователь зарегистрирован: test_simple_user
ℹ️  Пользователь уже существует: Пользователь
ℹ️  Kerberos пользователь уже существует: Пользователь
```

## Преимущества

1. **Автоматизация** - не требует ручной регистрации пользователей
2. **Безопасность** - пользователи регистрируются только при аутентификации
3. **Гибкость** - роли и отделы определяются автоматически
4. **Отказоустойчивость** - система проверяет существование перед регистрацией
5. **Логирование** - все действия записываются в лог

## Использование

### Для пользователей

1. **Первый вход** - пользователь просто заходит на сайт
2. **Автоматическая регистрация** - система регистрирует пользователя
3. **Последующие входы** - система узнает пользователя

### Для администраторов

1. **API проверки** - `/api/users/check-registration` - проверить регистрацию
2. **API регистрации** - `/api/users/register` - ручная регистрация
3. **Статистика** - `/api/statistics` - общая статистика пользователей

## Заключение

Система автоматической регистрации пользователей успешно реализована и протестирована. Пользователи автоматически регистрируются при первом входе в систему, что упрощает управление пользователями и обеспечивает безопасность.
