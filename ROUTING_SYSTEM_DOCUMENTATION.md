# Маршрутизация стилей, картинок и HTML для пользователей и админов

## Обзор

Настроена полная ролевая система маршрутизации для стилей, картинок и HTML контента. Система автоматически определяет роль пользователя и загружает соответствующие ресурсы.

## Структура файлов

### 📁 Организация по ролям

```
site/
├── admin-pages/           # Ресурсы для администраторов
│   ├── main-pg/
│   │   ├── img/          # Изображения для админов
│   │   ├── style.css     # Стили для админов
│   │   └── index.html    # HTML для админов
│   ├── questions-pg/
│   ├── all-lessons-pg/
│   ├── lessons-content-pg/
│   ├── users-info-pg/
│   └── templates/        # Шаблоны для админов
│       ├── css/
│       │   ├── header-footer.css
│       │   └── override-fonts.css
│       └── partials/
│           ├── header.html
│           └── footer.html
│
├── user-pages/            # Ресурсы для обычных пользователей
│   ├── main-pg/
│   │   ├── img/          # Изображения для пользователей
│   │   ├── style.css     # Стили для пользователей
│   │   └── index.html    # HTML для пользователей
│   ├── questions-pg/
│   ├── all-lessons-pg/
│   ├── lessons-content-pg/
│   ├── users-info-pg/
│   └── templates/        # Шаблоны для пользователей
│       ├── css/
│       │   ├── header-footer.css
│       │   └── override-fonts.css
│       └── partials/
│           ├── header.html
│           └── footer.html
```

## Маршрутизация

### 🎯 URL Patterns

| URL | Описание | Роль |
|-----|----------|------|
| `/main` | Главная страница | Автоматически по роли |
| `/questions` | Страница вопросов | Автоматически по роли |
| `/all-lessons` | Все уроки | Автоматически по роли |
| `/lessons-content` | База знаний | Автоматически по роли |
| `/users-info` | Информация о пользователях | Автоматически по роли |
| `/main-pg/img/лого.svg` | Изображения | Автоматически по роли |
| `/main-pg/style.css` | CSS стили | Автоматически по роли |
| `/templates/css/header-footer.css` | Шаблонные стили | Автоматически по роли |

### 🔧 Логика маршрутизации

#### 1. Определение роли пользователя

```python
def _determine_user_role(self, username: str) -> str:
    """Определение роли пользователя из БД"""
    # 1. Ищем пользователя в БД KerberosUser
    # 2. Если найден - возвращаем роль из БД
    # 3. Если не найден - используем хардкод
    # 4. Fallback к 'user' по умолчанию
```

#### 2. Выбор ресурсов по роли

```python
def _get_pages_for_user_role():
    """Получение страниц на основе роли пользователя"""
    if user_role == 'admin':
        return admin_pages, admin_template_dir
    else:
        return user_pages, user_template_dir
```

#### 3. Маршрутизация статических файлов

```python
@app.get("/<legacy_dir>/<path:legacy_path>")
def serve_legacy_asset(legacy_dir: str, legacy_path: str):
    # Определяем роль пользователя
    # Выбираем базовую папку (admin-pages или user-pages)
    # Загружаем файл из соответствующей папки
```

## Реализация

### 📝 Файл: `backend/routes.py`

#### Основные маршруты:

1. **Страницы** - `/main`, `/questions`, etc.
2. **Статические файлы** - `/main-pg/img/лого.svg`, `/main-pg/style.css`
3. **Шаблоны** - `/templates/css/header-footer.css`
4. **API** - `/api/user`, `/api/statistics`

#### Ролевая логика:

```python
def _get_pages_for_user_role():
    """Get pages map based on current user role."""
    user_info = g.get('user_info', {})
    user_role = user_info.get('role', 'user')
    
    if user_role == 'admin':
        return admin_pages, os.path.join(base_path, "admin-pages")
    else:
        return user_pages, os.path.join(base_path, "user-pages")
```

### 🔐 Файл: `backend/simplified_real_kerberos_auth.py`

#### Определение роли из БД:

```python
def _determine_user_role(self, username: str) -> str:
    """Определение роли пользователя из БД"""
    # 1. Поиск в KerberosUser таблице
    # 2. Fallback к хардкоду
    # 3. По умолчанию 'user'
```

## Тестирование

### ✅ Результаты тестов

#### Пользовательская роль (`user`):
- ✅ Страница отображается как пользовательская
- ✅ CSS файлы загружаются (200)
- ✅ Изображения загружаются (200)
- ✅ API возвращает роль `user`
- ✅ Все страницы доступны

#### Админская роль (`admin`):
- ✅ Страница отображается как админская
- ✅ CSS файлы загружаются (200)
- ✅ Изображения загружаются (200)
- ✅ API возвращает роль `admin`
- ✅ Все страницы доступны

### 📊 Статистика тестирования

| Ресурс | Статус | Описание |
|--------|--------|----------|
| `/main-pg/style.css` | ✅ 200 | CSS стили загружаются |
| `/main-pg/img/лого.svg` | ✅ 200 | Логотип загружается |
| `/main-pg/img/vector.svg` | ✅ 200 | Иконки загружаются |
| `/templates/css/header-footer.css` | ✅ 200 | Шаблонные стили |
| `/templates/css/override-fonts.css` | ✅ 200 | Шрифты |
| `/main` | ✅ 200 | Главная страница |
| `/questions` | ✅ 200 | Страница вопросов |
| `/all-lessons` | ✅ 200 | Все уроки |
| `/lessons-content` | ✅ 200 | База знаний |
| `/users-info` | ✅ 200 | Пользователи |

## Преимущества

### 🎯 Ролевая система

1. **Автоматическое определение роли** - система сама определяет роль пользователя
2. **Разделение контента** - админы и пользователи видят разный контент
3. **Единая кодовая база** - один код для обеих ролей
4. **Гибкость** - легко добавлять новые роли

### 🔧 Технические преимущества

1. **Кэширование** - статические файлы кэшируются браузером
2. **Безопасность** - доступ только к разрешенным ресурсам
3. **Производительность** - быстрая загрузка ресурсов
4. **Масштабируемость** - легко добавлять новые страницы

### 📱 Пользовательский опыт

1. **Консистентность** - единый стиль для каждой роли
2. **Интуитивность** - понятная навигация
3. **Адаптивность** - работает на всех устройствах
4. **Скорость** - быстрая загрузка страниц

## Использование

### Для разработчиков

1. **Добавление новых страниц**:
   - Создать папку в `admin-pages/` и `user-pages/`
   - Добавить в `ALLOWED_PAGE_DIRS` в `config.py`

2. **Изменение стилей**:
   - Редактировать CSS в соответствующих папках
   - Стили автоматически применяются по роли

3. **Добавление изображений**:
   - Поместить в `img/` папку соответствующей роли
   - Использовать относительные пути в HTML

### Для администраторов

1. **Управление ролями**:
   - Изменить роль в таблице `KerberosUser`
   - Система автоматически применит изменения

2. **Мониторинг**:
   - Логи показывают загружаемые ресурсы
   - API предоставляет информацию о пользователе

## Заключение

Ролевая система маршрутизации полностью настроена и протестирована. Система автоматически:

- ✅ Определяет роль пользователя из БД
- ✅ Загружает соответствующие ресурсы
- ✅ Отображает правильный контент
- ✅ Обеспечивает безопасность доступа
- ✅ Предоставляет отличный пользовательский опыт

Все стили, картинки и HTML контент теперь правильно маршрутизируются в зависимости от роли пользователя!
