# 🔐 MAC Address Authentication Documentation

## 📋 Обзор

MAC Address Authentication - это система аутентификации, которая определяет пользователей по MAC-адресу их сетевой карты. Это особенно полезно в корпоративных сетях, где каждому компьютеру соответствует определенный пользователь.

## 🚀 Как это работает

### 1. Процесс аутентификации

1. **Получение MAC адреса** - сервер определяет MAC адрес клиента
2. **Поиск в базе данных** - ищет этот MAC в базе данных пользователей
3. **Присвоение роли** - если найден, присваивает соответствующую роль
4. **Резервная аутентификация** - если не найден, использует Windows Auth

### 2. Структура данных

```python
mac_database = {
    'F4:6D:04:44:8C:8B': {  # MAC адрес
        'username': 'Пользователь',     # Имя пользователя
        'role': 'admin',               # Роль (admin/user)
        'full_name': 'Пользователь Системы',  # Полное имя
        'department': 'IT отдел'       # Отдел
    }
}
```

## 🔧 Настройка

### 1. Добавление нового пользователя

Отредактируйте файл `site/backend/mac_auth.py`:

```python
mac_database = {
    # Существующие пользователи...
    'AA:BB:CC:DD:EE:FF': {  # Новый MAC адрес
        'username': 'new_user',
        'role': 'admin',  # или 'user'
        'full_name': 'Новый Пользователь',
        'department': 'Новый Отдел'
    }
}
```

### 2. Получение MAC адреса

Запустите утилиту для получения MAC адреса:

```bash
cd site
python get_mac_address.py
```

### 3. Конфигурация

Переменные окружения:

```bash
# Включить MAC аутентификацию
MAC_AUTH_ENABLED=true

# Включить отладочный режим
MAC_AUTH_DEBUG=true
```

## 🧪 Тестирование

### 1. Запуск тестов

```bash
cd site
python test_mac_auth.py
```

### 2. Отладочные endpoints

- `/debug/mac` - отладочная информация MAC аутентификации
- `/api/user` - информация о текущем пользователе

### 3. Проверка работы

1. Запустите сервер: `python run.py`
2. Откройте браузер: `http://127.0.0.1:5000`
3. Проверьте отладку: `http://127.0.0.1:5000/debug/mac`

## 📊 Преимущества MAC аутентификации

### ✅ Плюсы

- **Уникальная идентификация** - каждый компьютер имеет уникальный MAC
- **Независимость от имени пользователя** - работает даже при смене пользователя
- **Стабильность** - MAC не меняется при смене IP адреса
- **Корпоративная безопасность** - подходит для строгих корпоративных политик
- **Автоматизация** - не требует ввода паролей

### ⚠️ Ограничения

- **Локальная сеть** - работает только в локальной сети
- **NAT/Прокси** - не работает через NAT или прокси серверы
- **Подделка** - MAC адрес можно подделать
- **Замена оборудования** - нужно обновлять при замене сетевой карты
- **Мобильные устройства** - не подходит для мобильных устройств

## 🔒 Безопасность

### 1. Уровни защиты

- **MAC фильтрация** - доступ только с разрешенных MAC адресов
- **Резервная аутентификация** - Windows Auth как fallback
- **Логирование** - все попытки доступа записываются в логи

### 2. Рекомендации

- Используйте в комбинации с другими методами аутентификации
- Регулярно обновляйте базу данных MAC адресов
- Мониторьте логи на предмет подозрительной активности

## 🌐 Сетевые сценарии

### 1. Локальная сеть

```
Компьютер A (MAC: AA:BB:CC:DD:EE:FF) → Сервер → Проверка MAC → Доступ
```

### 2. Через NAT

```
Компьютер A → NAT → Сервер → MAC не определяется → Windows Auth
```

### 3. Через прокси

```
Компьютер A → Прокси → Сервер → MAC не определяется → Windows Auth
```

## 📝 API Reference

### Получение информации о пользователе

```http
GET /api/user
```

Ответ:
```json
{
  "username": "Пользователь",
  "role": "admin",
  "mac_address": "F4:6D:04:44:8C:8B",
  "auth_method": "mac_auth",
  "department": "IT отдел",
  "full_name": "Пользователь Системы"
}
```

### Отладочная информация

```http
GET /debug/mac
```

Ответ:
```json
{
  "user_info": {
    "username": "Пользователь",
    "role": "admin",
    "mac_address": "F4:6D:04:44:8C:8B"
  },
  "debug_info": {
    "client_mac": "F4:6D:04:44:8C:8B",
    "local_mac": "F4:6D:04:44:8C:8B",
    "system_info": {
      "platform": "Windows",
      "hostname": "Valdemar"
    }
  }
}
```

## 🛠️ Утилиты

### 1. get_mac_address.py

Получение MAC адреса вашего компьютера:

```bash
python get_mac_address.py
```

### 2. test_mac_auth.py

Тестирование MAC аутентификации:

```bash
python test_mac_auth.py
```

## 🔄 Интеграция с Windows Auth

MAC аутентификация работает в связке с Windows аутентификацией:

1. **Приоритет MAC** - сначала проверяется MAC адрес
2. **Fallback Windows** - если MAC не найден, используется Windows Auth
3. **Гибридный подход** - можно использовать оба метода одновременно

## 📈 Мониторинг

### Логи

Проверяйте логи в `backend/logs/app.log`:

```
INFO - MAC Authentication initialized
INFO - User authenticated via MAC: F4:6D:04:44:8C:8B
INFO - Role assigned: admin
```

### Метрики

- Количество успешных MAC аутентификаций
- Количество fallback на Windows Auth
- Попытки доступа с неизвестных MAC адресов

## 🆘 Troubleshooting

### Проблема: MAC адрес не определяется

**Решение:**
1. Проверьте, что вы в локальной сети
2. Убедитесь, что нет NAT/прокси
3. Проверьте права доступа к ARP таблице

### Проблема: Пользователь не найден

**Решение:**
1. Добавьте MAC адрес в базу данных
2. Проверьте формат MAC адреса
3. Убедитесь в правильности регистра

### Проблема: Не работает в продакшене

**Решение:**
1. Проверьте конфигурацию сервера
2. Убедитесь в доступности ARP таблицы
3. Проверьте права доступа к сетевому интерфейсу

## 📚 Дополнительные ресурсы

- [RFC 826 - ARP Protocol](https://tools.ietf.org/html/rfc826)
- [MAC Address - Wikipedia](https://en.wikipedia.org/wiki/MAC_address)
- [Network Authentication Best Practices](https://example.com)

---

**Статус:** ✅ Реализовано и протестировано  
**Версия:** 1.0  
**Дата:** 2025-10-05


