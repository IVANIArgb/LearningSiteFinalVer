# Быстрый старт - Ролевая система

## Что сделано

✅ **Система готова к работе!** 

Теперь при заходе на сайт:
- **Админы** (admin, administrator, root, manager, админ) → видят шаблоны из `admin-pges`
- **Обычные пользователи** → видят шаблоны из `user-ges`

## Как запустить

```bash
cd admin-pges
python run.py
```

Откройте: `http://127.0.0.1:5000`

## Как протестировать

### Вариант 1: Через браузер
1. Запустите сервер
2. Откройте браузер
3. Система автоматически определит вашу роль по Windows Authentication

### Вариант 2: Через тесты
```bash
cd admin-pges
python test_role_system.py
```

### Вариант 3: Отладка
Перейдите на `/debug/auth` для просмотра информации об аутентификации

## Что изменилось

### В коде:
- `backend/auth.py` - добавлено определение ролей
- `backend/config.py` - добавлены пути к шаблонам
- `backend/routes.py` - добавлена ролевая маршрутизация

### Новые файлы:
- `test_role_system.py` - тесты системы
- `ROLE_SYSTEM_DOCUMENTATION.md` - полная документация

## Структура шаблонов

```
Learning Site Final/
├── admin-pges/     ← Шаблоны для админов (текущие)
│   ├── main-pg/
│   ├── all-courses-pg/
│   └── ...
└── user-ges/       ← Шаблоны для пользователей (ваши новые)
    ├── main-pg/
    ├── all-courses-pg/
    └── ...
```

## Добавление новых админов

Отредактируйте `backend/auth.py`, метод `_determine_user_role()`:

```python
admin_users = [
    'admin', 'administrator', 'root', 'manager',
    'админ', 'администратор', 'руководитель',
    'ваш_новый_админ'  # Добавьте сюда
]
```

## Готово! 🎉

Система полностью настроена и готова к использованию. Просто запустите сервер и протестируйте с разными пользователями.
